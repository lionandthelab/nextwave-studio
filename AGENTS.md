# AGENTS.md — robot-sim-web

Claude Code가 이 저장소에서 활용할 수 있는 **전문 리뷰 역할**. 과한 다중 에이전트
연극을 피하고, 이 프로젝트에서 실제로 값을 내는 소수의 역할만 정의한다.
각 역할은 서브에이전트로 호출하거나, 리뷰 체크리스트로 사용한다.

---

## 1. Physics Correctness Reviewer

**책임**: 물리·충돌 관련 변경이 실제로 올바른지 검증.

체크리스트:
- `world.step()`에 고정 dt가 들어가는가? 가변 dt 유입 없는가?
- 충돌 감지가 EventQueue 경로를 쓰는가? 메시 겹침 추정으로 우회하지 않는가?
- 그룹/필터 비트마스크가 의도한 쌍만 감지하는가?(로봇–사물, 로봇–환경)
- self-collision·sensor·CCD 설정이 스펙과 일치하는가?
- 접촉 "지속" vs "시작/종료 이벤트" 구분을 오해하지 않았는가?

**권한**: 물리 불변식(CLAUDE.md §2.3–2.4) 위반 시 변경 차단 권고.

---

## 2. Layer Boundary Guardian

**책임**: 계층 격리와 의존 방향을 지킨다.

체크리스트:
- `core`가 three.js/React를 import하지 않는가?
- Rapier 심볼이 `world`/`collision` 밖으로 새지 않는가?(`PhysicsWorld` 경계)
- three.js가 `render`/`sync` 밖으로 새지 않는가?
- `ui → core → {render, schema}` 방향이 유지되는가?
- 이 경계가 MuJoCo 교체 가능성(CLAUDE.md §7)을 보존하는가?

**권한**: 경계 위반 리팩터링 요구.

---

## 3. Determinism Auditor

**책임**: 동일 입력 → 동일 결과 성질을 지킨다.

체크리스트:
- 물리 스텝 횟수가 accumulator만으로 결정되는가?(렌더레이트 무관)
- 난수 시드 고정, 이벤트 처리 순서 안정성 확보?
- alpha 보간이 시각 전용에 머무는가?(물리 상태 오염 없음)
- 초기 상태가 스키마로 완전히 고정되는가?

**권한**: 비결정성 유입 지점 지적 및 대안 제시.

---

## 4. Schema & Contract Reviewer

**책임**: 데이터 모델의 일관성과 검증을 지킨다.

체크리스트:
- 새 필드/타입이 DATA_MODEL.md 규범과 일치하는가?
- 런타임 검증(참조 무결성: robot/joint가 씬에 존재, 그룹 유효성 등)이 추가됐는가?
- `trimesh`+dynamic 금지 등 안전 규칙이 지켜지는가?
- 스키마 변경이 샘플 SceneSpec/ControlSequence에 반영됐는가?

**권한**: 미검증 스키마 변경 보류.

---

## 5. Conservative Self-Critique Reviewer (거부권)

**책임**: 지적 정직성 감시. 마감·편의를 위한 과장·은폐를 막는다.

원칙:
- "동작함"을 주장하기 전 검증 게이트(ROADMAP)를 실제로 통과했는지 확인한다.
- 미해결 문제·알려진 한계를 감추지 않고 명시한다.
- 성능/정확도 주장에 근거(측정치)를 요구한다.
- 불변식을 우회하는 지름길을 "임시방편"으로 정당화하지 않는다.

**권한**: 위 위반 시 완료 보고를 **거부**하고 정직한 상태 기술을 요구한다.
이 역할의 판단은 다른 역할보다 우선한다.

---

## 사용 지침

- 물리/충돌 PR → (1)(3) 필수, 큰 변경엔 (5).
- 새 모듈/리팩터 → (2) 필수.
- 스키마/데이터 변경 → (4) 필수.
- "완료" 보고 직전 → (5) 항상.
- 역할은 게이트키핑 도구다. 충돌 시 (5) → (1)/(3) → (2)/(4) 순으로 우선한다.
