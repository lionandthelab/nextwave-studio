# JOURNAL.md — 시도 기록 (append-only)

하네스 4축 중 **상태**의 절반. `TASKS.jsonl`이 "무엇이 남았나"라면
이 파일은 "무엇을 이미 해봤고 왜 안 됐나"다.

세션은 죽으면서 컨텍스트를 전부 잃는다. 다음 세션이 같은 막다른 길을 다시 걷지 않게
하는 유일한 장치가 이 파일이다. 그래서 **실패가 성공보다 중요하게 기록된다** —
성공은 커밋이 이미 말해주지만, 실패는 되돌려져서 저장소에 흔적이 남지 않는다.

## 규칙

- **필자는 `harness/outer-loop.sh` 하나다.** 세션은 읽기만 한다.
  두 필자가 append하면 순서가 섞이고, 섞인 저널은 다음 시도를 잘못 이끈다.
- 세션은 `node harness/hx.mjs journal <태스크-id>`로 **자기 태스크의 기록만** 읽는다.
  저널 전체를 컨텍스트에 넣으면 저널이 저널을 태운다.
- 사람이 손으로 관찰을 남겨도 된다. 형식만 맞추면 된다 —
  `## <시각> · <태스크-id> · <무슨 일>`.

---
